# ===================================================
# Quarkus Ledger — Merkle Verification Example
# ===================================================

quarkus.application.name=ledger-example-merkle-verification

# Datasource — H2 in-memory for dev/test
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:merkledb;DB_CLOSE_DELAY=-1

# Hibernate ORM
quarkus.hibernate-orm.database.generation=none

# Flyway — picks up migrations from both this app and quarkus-ledger JAR
quarkus.flyway.migrate-at-start=true
quarkus.flyway.locations=classpath:db/migration

# quarkus-ledger — Merkle enabled
quarkus.ledger.enabled=true
quarkus.ledger.hash-chain.enabled=true
quarkus.ledger.retention.enabled=false

# Activate AuditEntryRepository (extends @Alternative JpaLedgerEntryRepository)
quarkus.arc.selected-alternatives=io.quarkiverse.ledger.example.merkle.AuditEntryRepository

# HTTP — use 0 (random port) in test so we don't clash with other running apps
%test.quarkus.http.test-port=0

# Logging
quarkus.log.category."io.quarkiverse.ledger".level=DEBUG
